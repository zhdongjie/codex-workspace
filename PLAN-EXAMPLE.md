# 开发计划表

## 当前阶段：[填写阶段名称，如：用户模块开发]

### 任务列表
- [x] 分析既有数据库表结构
- [ ] 使用 create-endpoint 技能创建 /api/users 接口
- [ ] 使用 write-test 技能为 UserService 编写单元测试
- [ ] 使用 create-component 技能开发前端 UserProfile.vue 视图界面

> AI 执行须知：请严格按照从上到下的顺序执行未打钩 `[ ]` 的任务。完成一项后将其改为 `[x]` 并停止，等待人类下达下一步指令。